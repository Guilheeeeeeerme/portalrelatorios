# Gemini Instructions

Follow the repository rules in `AGENTS.md`.

Key constraints:

- Keep the project scoped to the empreendimento dump and format pipeline.
- Prefer minimal Node.js changes using built-in modules where practical.
- Avoid touching generated JSON unless the user asks for regeneration.
