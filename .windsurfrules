# Windsurf Rules

Follow `AGENTS.md`.

- The only supported npm commands are `dump:empreendimento-data`, `format:empreendimento-data`, and `full:empreendimento-data`.
- Keep dependencies minimal and do not restore removed Fastify or Playwright tooling unless explicitly requested.
- Treat `.env` values as secrets.
