# Claude Instructions

Follow the repository rules in `AGENTS.md`.

Key constraints:

- Keep the project scoped to `dump:empreendimento-data`, `format:empreendimento-data`, and `full:empreendimento-data`.
- Do not reintroduce removed server, Playwright, or capture tooling without explicit user request.
- Do not commit secrets or broad generated-data churn.
