# Cline Rules

Follow `AGENTS.md`.

- Preserve generated data unless the task requires changing it.
- Validate script syntax with `node --check` after code edits.
- Run live dump/full commands only when a valid Power BI token is available.
